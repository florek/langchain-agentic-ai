# Klucz odpowiedzi – 25.09.2026

Skład: **10 pytań z ostatniej lekcji + 10 pytań z wcześniejszych lekcji**.

1. B — Ollama upraszcza pobieranie, przechowywanie i lokalne uruchamianie modeli.
2. D — `PromptTemplate` jest opakowaniem promptu, które przyjmuje parametry i formatuje wejście dla modelu.
3. A — Open-weight oznacza dostępność wag do pobrania, co pozwala uruchomić model we własnym środowisku.
4. C — Model chat przyjmuje listę ustrukturyzowanych wiadomości i zwraca wiadomość AI.
5. D — Dzięki wspólnej abstrakcji zwykle zmienia się klienta integracji i nazwę modelu, a reszta łańcucha pozostaje podobna.
6. B — LCEL przekazuje wynik jednego komponentu jako wejście następnego i tworzy z nich Runnable.
7. C — `ollama pull gemma3:270m` pobiera wskazany wariant modelu.
8. A — Klucze słownika przekazanego do `invoke` muszą odpowiadać zmiennym wymaganym przez szablon; niezgodność powoduje błąd walidacji.
9. B — Mały model ma mniejsze wymagania i może działać szybciej, ale zwykle odpowiada gorzej niż większy model.
10. D — RAG dostarcza kontekst z dokumentów, natomiast agent dodatkowo decyduje o działaniach i użyciu narzędzi.
11. A — Model lokalny wspiera prywatność, kontrolę, pracę offline i brak opłaty za każde wywołanie.
12. C — „Lost in the middle” oznacza, że model może gorzej wykorzystywać informacje umieszczone w środku długiego kontekstu.
13. D — `ollama list` wyświetla modele dostępne lokalnie.
14. B — Sekrety powinny pozostawać w zmiennych środowiskowych, ponieważ zapisanie ich w repozytorium grozi ujawnieniem.
15. C — Nazwa przekazana do `ChatOllama` musi odpowiadać modelowi, który jest dostępny w Ollama.
16. A — Structured tool calling przekazuje nazwę funkcji i argumenty w formie, którą runtime może bezpośrednio obsłużyć.
17. B — Ujednolicony interfejs modelu oddziela logikę aplikacji od konkretnego backendu.
18. D — Nawet dobry generator nie naprawi odpowiedzi, jeśli retriever dostarczy nieistotny lub słaby kontekst.
19. A — `ChatOllama` łączy aplikację LangChain z lokalnym serwerem Ollama obsługującym model Gemma 3.
20. C — Graf LangGraph składa się z węzłów, krawędzi i współdzielonego stanu przekazywanego między węzłami.
