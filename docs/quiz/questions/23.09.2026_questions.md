# Quiz LangChain Agentic AI – 23.09.2026

Skład: **10 pytań z ostatniej lekcji** (PromptTemplate, LCEL, ChatOpenAI/ChatOllama) + **10 z wcześniejszych notatek** (wprowadzenie do Agentic AI).

---

## Pytanie 1 [łatwe] [LLM]

Do czego służy `load_dotenv()` przed utworzeniem klienta modelu w chmurze?

A) Trenuje lokalny model na danych z `.env`  
B) Kompiluje szablon promptu do bytecode  
C) Ładuje zmienne środowiskowe (np. klucz API) do procesu  
D) Wymusza `temperature=0` dla wszystkich wywołań

---

## Pytanie 2 [łatwe] [LangChain]

`PromptTemplate` wymaga przede wszystkim:

A) stringa `template` z placeholderami oraz listy `input_variables`  
B) wyłącznie obiektu `ChatOpenAI` bez szablonu  
C) pliku PDF jako jedynego źródła promptu  
D) grafu stanów LangGraph zamiast stringa

---

## Pytanie 3 [średnie] [LangChain]

Po złożeniu `prompt_template | llm` powstaje:

A) wyłącznie surowy string HTML  
B) baza wektorowa FAISS  
C) agent multi-agentowy z pamięcią długoterminową  
D) łańcuch Runnable (LCEL): najpierw szablon, potem model

---

## Pytanie 4 [średnie] [LLM]

Ustawienie `temperature=0` przy modelu chat zwykle oznacza:

A) całkowite wyłączenie generowania tekstu  
B) dążenie do bardziej deterministycznych, powtarzalnych odpowiedzi  
C) obowiązkowe użycie wyłącznie Ollamy  
D) automatyczne włączenie tool calling

---

## Pytanie 5 [łatwe] [LangChain]

Tekst odpowiedzi modelu chat z łańcucha odczytuje się typowo przez:

A) atrybut `.content` obiektu odpowiedzi  
B) metodę `.fit()` na łańcuchu  
C) pole `.vector_store`  
D) plik `requirements.txt`

---

## Pytanie 6 [średnie] [LangChain]

Wywołanie `chain.invoke(...)` dla szablonu z placeholderem `{information}` powinno dostać:

A) listę samych liczb bez kluczy  
B) pusty string zamiast słownika  
C) słownik z kluczem dopasowanym do `input_variables` (np. `information`)  
D) wyłącznie obiekt `ChatOllama` jako jedyny argument

---

## Pytanie 7 [średnie] [LLM]

`ChatOpenAI` i `ChatOllama` różnią się głównie tym, że:

A) tylko `ChatOllama` obsługuje placeholdery w prompcie  
B) tylko `ChatOpenAI` może być użyty w LCEL  
C) `ChatOpenAI` nie przyjmuje parametru `model`  
D) jeden korzysta z API w chmurze, drugi z lokalnego serwera Ollama

---

## Pytanie 8 [trudne] [LangChain]

Dlaczego warto użyć `PromptTemplate` zamiast ręcznego sklejania f-stringa tuż przed wywołaniem API?

A) f-stringi są zabronione w Pythonie 3.11+  
B) szablon jest Runnable i da się go złożyć w łańcuch oraz wielokrotnie wywoływać z różnymi danymi  
C) `PromptTemplate` automatycznie buduje bazę wektorową  
D) bez szablonu `temperature` zawsze wynosi 1

---

## Pytanie 9 [trudne] [LLM]

Typowa konsekwencja braku załadowanych zmiennych środowiskowych przy `ChatOpenAI`:

A) łańcuch LCEL zamienia się automatycznie w LangGraph  
B) model lokalny Ollama startuje bez instalacji  
C) wywołanie kończy się błędem braku klucza / brakiem autoryzacji  
D) `invoke` zwraca zawsze pusty `.content` bez błędu

---

## Pytanie 10 [trudne] [LangChain]

Co pozostaje niezmienne przy przełączeniu backendu z `ChatOpenAI` na `ChatOllama` w minimalnym pipeline prompt → model?

A) ten sam wzorzec: szablon, złożenie `prompt | llm`, `invoke` ze słownikiem  
B) obowiązkowa zmiana wszystkich `input_variables` na liczby  
C) konieczność usunięcia `PromptTemplate` i pisania wyłącznie f-stringów  
D) wymiana operatora `|` na osobny serwer MCP

---

## Pytanie 11 [łatwe] [LLM]

Które zestawienie najlepiej opisuje trzy poziomy aplikacji LLM?

A) klasyczny LLM = narzędzia; RAG = tylko temperature; agent = sam f-string  
B) klasyczny LLM = jednorazowy prompt→model; RAG = model + wyszukiwanie wiedzy; agent = decyzje, narzędzia, kontekst  
C) RAG i agent to zawsze to samo podejście bez różnic  
D) agent nigdy nie korzysta z modelu językowego

---

## Pytanie 12 [łatwe] [LLM]

**Agentic AI** w sensie kursu oznacza systemy, w których model:

A) generuje wyłącznie jeden token i kończy pracę  
B) działa tylko offline bez żadnego API  
C) nie może korzystać z pamięci ani narzędzi  
D) decyduje o kolejnych krokach, może używać narzędzi i utrzymywać kontekst

---

## Pytanie 13 [średnie] [LangChain]

LangChain vs LangGraph — które stwierdzenie jest poprawne?

A) LangChain składa aplikacje LLM (prompty, modele, Runnable); LangGraph buduje przepływy agentowe (grafy stanów, pętle)  
B) LangGraph zastępuje Pythona jako język programowania  
C) LangChain służy wyłącznie do trenowania sieci neuronowych od zera  
D) LangGraph to baza wektorowa konkurencyjna wobec FAISS

---

## Pytanie 14 [średnie] [LLM]

**Context engineering** dotyczy przede wszystkim:

A) wyłącznie wyboru koloru motywu UI  
B) kompilacji modelu do C++  
C) zarządzania tym, co trafia do kontekstu modelu (dokumenty, historia, wyniki narzędzi, limity okna)  
D) zastąpienia prompt engineeringu przez losowe tokeny

---

## Pytanie 15 [średnie] [LLM]

Few-shot prompting polega na:

A) usunięciu wszystkich przykładów z promptu  
B) trenowaniu modelu od zera na GPU  
C) wyłączeniu pamięci agenta  
D) umieszczeniu w prompcie przykładów pokazujących oczekiwany format lub styl

---

## Pytanie 16 [trudne] [LLM]

Która różnica RAG vs agent jest najtrafniejsza?

A) RAG dostarcza wiedzę z dokumentów; agent dodatkowo steruje przebiegiem i może używać narzędzi  
B) agent nigdy nie korzysta z dokumentów, a RAG zawsze wywołuje zewnętrzne API płatności  
C) RAG wymaga LangGraph, a agent zabania LangChain  
D) nie ma żadnej różnicy — to synonimy

---

## Pytanie 17 [średnie] [Narzędzia]

LangSmith w ekosystemie kursu służy głównie do:

A) hostowania lokalnych wag modelu Ollama  
B) tracingu i obserwowalności przebiegów LLM  
C) zastąpienia Gita jako systemu kontroli wersji  
D) automatycznego generowania baz FAISS bez danych

---

## Pytanie 18 [trudne] [LLM]

ReAct prompting to wzorzec:

A) wyłącznie formatowania JSON bez rozumowania  
B) trenowania regresji liniowej na embeddingach  
C) wymiany LangChain na czysty SQL  
D) „rozumowanie + działanie” (thought / action / observation) przy agentach z narzędziami

---

## Pytanie 19 [łatwe] [LLM]

Czy do zrozumienia materiału kursu wymagana jest wcześniejsza znajomość Machine Learningu?

A) Tak — bez ML kurs jest niedostępny  
B) Tak — wymagany jest doktorat z deep learningu  
C) Nie — wystarczy m.in. Python, Git, venv i zmienne środowiskowe  
D) Nie — wymagany jest wyłącznie certyfikat LangSmith

---

## Pytanie 20 [trudne] [LLM]

Typowa pułapka koncepcyjna przy ocenie systemu GenAI:

A) uznanie każdego wywołania LLM za „agenta”, mimo braku narzędzi, pętli decyzji i pamięci  
B) rozróżnianie RAG od klasycznego prompt→model  
C) używanie LangSmith do obserwacji przebiegów  
D) projektowanie jasnego kontraktu wejścia/wyjścia w łańcuchu
