# Quiz LangChain Agentic AI – 24.09.2026

Skład: **10 pytań z ostatniej lekcji** (PromptTemplate, LCEL, ChatOpenAI/ChatOllama, invoke) + **10 z wcześniejszych notatek** (Agentic AI, RAG, context, narzędzia, konfiguracja).

---

## Pytanie 1 [łatwe] [LangChain]

Co robi operator `|` w LCEL, gdy łączysz szablon promptu z modelem chat?

A) Trenuje model od zera na Twoim dysku  
B) Zapisuje klucz API do pliku `.gitignore`  
C) Składa komponenty w łańcuch Runnable: najpierw szablon, potem model  
D) Wymusza użycie wyłącznie bazy wektorowej Pinecone

---

## Pytanie 2 [łatwe] [LLM]

Czym jest **tool calling** w systemach agentowych?

A) Mechanizm, w którym model zwraca strukturalne żądanie wywołania funkcji zamiast samego tekstu  
B) Wyłącznie formatowanie CSS strony quizu  
C) Trenowanie embeddingów bez dokumentów  
D) Zamiana LangGraph na zwykły f-string

---

## Pytanie 3 [średnie] [LLM]

Parametr `temperature` w modelu chat steruje przede wszystkim:

A) liczbą plików w `.venv`  
B) losowością / kreatywnością odpowiedzi modelu  
C) rozmiarem chunków w RAG  
D) wyborem menedżera pakietów uv zamiast pip

---

## Pytanie 4 [średnie] [LLM]

**MCP** (Model Context Protocol) to:

A) baza wektorowa konkurencyjna wobec FAISS  
B) język programowania zamiast Pythona  
C) wyłącznie narzędzie do formatowania kodu Black  
D) otwarty standard łączenia LLM z zewnętrznymi źródłami kontekstu i narzędziami

---

## Pytanie 5 [łatwe] [LangChain]

Skąd `PromptTemplate` bierze wartości do placeholderów (np. `{information}`) przy wywołaniu łańcucha?

A) Ze słownika przekazanego do `invoke`, z kluczami zgodnymi z `input_variables`  
B) Wyłącznie z nazwy pliku `uv.lock`  
C) Z losowych tokenów bez walidacji  
D) Z grafu stanów LangGraph — bez żadnego słownika

---

## Pytanie 6 [trudne] [LLM]

Efekt **lost in the middle** w context engineeringu oznacza, że:

A) model zawsze czyta tylko ostatni znak promptu  
B) RAG nigdy nie działa przy więcej niż jednym dokumencie  
C) modele gorzej wykorzystują informacje umieszczone w środku bardzo długiego kontekstu  
D) `temperature=0` usuwa całe okno kontekstowe

---

## Pytanie 7 [średnie] [LangChain]

Typowa pułapka przy odczycie odpowiedzi z łańcucha chat: drukowanie całego obiektu odpowiedzi zamiast `.content` powoduje, że:

A) model automatycznie włącza tool calling  
B) widać metadane obiektu odpowiedzi, a nie sam wygenerowany tekst dla użytkownika  
C) `PromptTemplate` przestaje być Runnable  
D) Ollama zawsze startuje na porcie 80

---

## Pytanie 8 [łatwe] [LLM]

**FAISS** w ekosystemie RAG to przede wszystkim:

A) biblioteka do szybkiego wyszukiwania podobieństwa (lokalnie w pamięci lub na dysku)  
B) platforma tracingu jak LangSmith  
C) protokół MCP do hostowania agentów w chmurze  
D) menedżer pakietów Python zamiast uv

---

## Pytanie 9 [trudne] [LLM]

Wysoka `temperature` przy zadaniu wymagającym stałej struktury odpowiedzi (np. „krótkie podsumowanie + dwa fakty”) najczęściej:

A) gwarantuje identyczny JSON przy każdym wywołaniu  
B) wyłącza potrzebę `PromptTemplate`  
C) wymusza użycie wyłącznie `ChatOllama`  
D) utrudnia uzyskanie stabilnego, powtarzalnego formatu odpowiedzi

---

## Pytanie 10 [średnie] [LLM]

`ConversationBufferMemory` vs `ConversationSummaryMemory` — różnica:

A) obie zawsze usuwają historię po pierwszej turze  
B) Buffer trzyma pełną historię; Summary kompresuje historię do podsumowania  
C) Summary działa tylko z Pinecone; Buffer tylko z FAISS  
D) Buffer wymaga LangGraph; Summary go zabania

---

## Pytanie 11 [łatwe] [LangChain]

Integracja `ChatOpenAI` pochodzi z pakietu:

A) `langchain-ollama`  
B) `python-dotenv` wyłącznie  
C) `langchain-openai`  
D) `black` / `isort`

---

## Pytanie 12 [trudne] [LLM]

Dlaczego natywne tool calling jest preferowane względem klasycznego tekstowego formatu ReAct, gdy model je obsługuje?

A) Mniej halucynacji formatu i większa niezawodność strukturalnych wywołań narzędzi  
B) Bo całkowicie usuwa potrzebę jakiegokolwiek rozumowania agenta  
C) Bo zastępuje wszystkie bazy wektorowe jednym plikiem CSV  
D) Bo wyłącza limity tokenów kontekstu

---

## Pytanie 13 [średnie] [LangChain]

Poprawna kolejność minimalnego przepływu end-to-end (prompt → model) to:

A) `invoke` → dopiero potem `load_dotenv` → na końcu szablon  
B) wyłącznie trenowanie modelu ML bez promptu  
C) najpierw LangSmith Evaluation, potem usunięcie `.env`  
D) `load_dotenv` → `PromptTemplate` → LLM → złożenie `prompt | llm` → `invoke` → odczyt `.content`

---

## Pytanie 14 [średnie] [LLM]

W pipeline RAG kluczowa decyzja projektowa — rozmiar chunków i strategia podziału — wpływa na jakość odpowiedzi:

A) wyłącznie na kolor wykresów w LangSmith  
B) często bardziej niż sam wybór modelu LLM  
C) tylko wtedy, gdy nie ma embeddingów  
D) wcale — chunking jest opcjonalną dekoracją bez skutków

---

## Pytanie 15 [trudne] [LangChain]

Jeśli klucze w słowniku przekazanym do `chain.invoke(...)` nie zgadzają się z `input_variables` szablonu:

A) łańcuch cicho pomija placeholdery i działa dalej bez błędu  
B) model lokalny Ollama nadpisuje wszystkie klucze  
C) zwykle pojawia się błąd walidacji wejścia  
D) operator `|` automatycznie tworzy nowy PromptTemplate

---

## Pytanie 16 [łatwe] [Narzędzia]

Polecenie `uv sync` w typowym przepływie projektu:

A) tworzy środowisko wirtualne i instaluje zależności z lockfile  
B) commituje plik `.env` z kluczami API do Gita  
C) uruchamia wyłącznie model GPT bez Pythona  
D) usuwa `pyproject.toml` i zastępuje go YAML

---

## Pytanie 17 [trudne] [LLM]

**Deep Agents** w sensie kursu to przede wszystkim:

A) pojedyncze wywołanie LLM bez pętli i narzędzi  
B) zaawansowane architektury z wielopoziomowym planowaniem i delegowaniem podzadań  
C) synonim samej bazy Chroma bez agentów  
D) wyłącznie styl formatowania Black

---

## Pytanie 18 [średnie] [LLM]

Po co ustawiać `temperature=0` przy zadaniach typu streszczenie i fakty?

A) Aby wyłączyć całkowicie generowanie tekstu  
B) Aby wymusić użycie tylko FAISS  
C) Aby zastąpić `invoke` metodą `fit`  
D) Aby dążyć do bardziej deterministycznych, powtarzalnych wyników

---

## Pytanie 19 [średnie] [LLM]

**Pruning tool results** w context engineeringu oznacza:

A) dodawanie wszystkich wyników narzędzi bez limitu długości  
B) trenowanie modelu na wynikach narzędzi  
C) obcinanie zbyt długich wyników narzędzi przed dołączeniem ich do kontekstu  
D) usuwanie instrukcji systemowej na rzecz historii czatu

---

## Pytanie 20 [trudne] [LangChain]

Dlaczego `PromptTemplate` jest lepszy od ręcznego f-stringa sklejanego tuż przed API, gdy budujesz aplikację w LangChain?

A) Szablon jest elementem Runnable: da się go złożyć w łańcuch i wielokrotnie wywoływać z różnymi danymi  
B) f-stringi są zabronione w Pythonie 3.12  
C) `PromptTemplate` automatycznie buduje indeks FAISS  
D) Bez szablonu `ChatOpenAI` nie przyjmuje parametru `model`
